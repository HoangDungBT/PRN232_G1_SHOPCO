@{
    ViewData["Title"] = "Quản Lý Đơn Hàng";
}

<div class="card shadow-sm border-0 mt-3">
    <div class="card-header bg-white d-flex justify-content-between align-items-center py-3">
        <h5 class="mb-0 fw-bold"><i class="fas fa-shopping-cart text-success me-2"></i> Danh Sách Đơn Hàng</h5>
        <div>
            <span class="badge bg-warning text-dark">5 Đơn chờ duyệt</span>
        </div>
    </div>
    <div class="card-body text-center text-muted py-5">
        <i class="fas fa-tools fa-3x mb-3 text-secondary"></i>
        <p>Bảng danh sách đơn hàng sẽ được gọi từ API và hiển thị ở đây.</p>
    </div>
</div>
